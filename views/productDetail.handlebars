<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>{{product.title}}</title>
  <script type="module" src="/js/cartActions.js"></script>
</head>
<body>
  <h1>{{product.title}}</h1>
  <p><strong>Precio:</strong> ${{product.price}}</p>
  <p><strong>Categoría:</strong> {{product.category}}</p>
  <p><strong>Descripción:</strong> {{product.description}}</p>
  <p><strong>Stock:</strong> {{product.stock}}</p>
  <p><strong>Estado:</strong> {{#if product.status}}Disponible{{else}}Agotado{{/if}}</p>

  <button onclick="addToCart('{{product._id}}', '{{cartId}}')">Agregar al carrito</button>

  <br /><br />
  <a href="/products">← Volver a la lista de productos</a>
</body>
</html>
